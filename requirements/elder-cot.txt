# Install this only in the cloned `elder-cot` environment.  Stage 1 remains
# pinned to transformers 4.52.3; GLM-4.1V requires the newer architecture.
transformers==4.57.1
accelerate>=1.1,<2
datasets>=2.21,<5
packaging>=23
pillow>=10
pyyaml>=6
safetensors>=0.4
sentencepiece>=0.2
