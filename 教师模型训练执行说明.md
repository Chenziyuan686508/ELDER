# 教师模型训练执行说明：Embed-RL 双端 CoT 多模态检索

版本：2026-09-07 · 第一阶段：实现、训练并验证显式 CoT 检索教师。

## 1. 交给执行 Codex 的任务

请在当前项目中实现并运行一个**双端 CoT 增强的多模态检索教师训练流程**。先检查项目、模型、数据及 GPU 环境，复用可用实现，完成数据审计、输入管线修复、训练冒烟测试、小规模对照实验和结果报告。

本文是独立执行规格，不要求读取此前聊天。下文目录、模块和命令是需要实现的接口，不代表这些代码已经存在。已有项目若采用其他结构，可保持其惯例并提供等价命令。

在现有已授权资源中主动完成工作。不要因普通实现选择反复要求用户确认；把实际选择及依据写进配置和报告。没有 GPU、模型权重、媒体或评测生成器时，继续完成不依赖它们的代码、数据检查和测试，准确列出阻塞项。不要购买计算资源，不要虚构已执行的训练或实验结果。

## 2. 目标与已确定设计

研究的最终目标是把教师的显式中间过程蒸馏到学生的一次前向 latent tokens。本次仅训练和验证教师，为之后的蒸馏提供可靠起点。

教师是一个共享参数的双塔 embedding 模型：

```text
query 自身原始文本/媒体 + query_cot ──→ 同一个教师编码器 ──→ e_query
candidate 自身原始文本/媒体 + pos_cot ─→ 同一个教师编码器 ──→ e_candidate
                                      ↓
                              双向检索对比损失
```

- 两端分别编码，禁止把 query 与 positive 拼成一个联合输入。
- candidate 的内容和 CoT 不依赖当前 query，支持同一任务下离线编码并复用候选向量。
- 训练 CoT 允许包含利用训练集正确答案生成的特权信息；这不是正常评测可使用的输入。
- 第一版直接使用现成 CoT，以检索对比损失训练教师。此教师学习**读取 CoT 并产生检索表示**，不要求它自己生成 CoT。
- 暂不加入 CoT 语言模型损失、GRPO、阶段检索损失、重构解码器、新的 STEP tokens、学生 LAT tokens 或额外多模态融合模块。
- 暂不按 CoT 中的框坐标做裁剪，也不按 CoT 额外选择视觉证据。所有实验使用相同的原始媒体处理预算。
- 后续可把自然 CoT 切成 K 块，提取块末尾的累积因果隐藏状态。K 初始可取 3，不赋予固定 P/G/R/S 语义。本次仅预留接口，不训练过程蒸馏。

**不能预先保证 CoT 带来收益。** 本次实验要回答：它是否超过相同数据和训练条件下的无 CoT 继续训练。仅 loss 下降、超过未训练基座或在答案辅助的验证 CoT 上变好，都不足以回答这个问题。

## 3. 数据与已核实事实

### 3.1 固定来源

- 数据：[ZoengHouNaam/Embed-RL-Train](https://huggingface.co/datasets/ZoengHouNaam/Embed-RL-Train/tree/af9e2e062e51f79df926909e7b9481fa697bfbcf/MMEB-train-contrastive-learning)。
- 数据 revision：`af9e2e062e51f79df926909e7b9481fa697bfbcf`。
- 使用子目录：`MMEB-train-contrastive-learning`；第一版不使用强化学习目录。
- 参考代码：[ZoengHN/Embed-RL](https://github.com/ZoengHN/Embed-RL/tree/1cb5b433395a709c1292df98d898083e75fb1057)。
- 代码 commit：`1cb5b433395a709c1292df98d898083e75fb1057`。
- 方法背景：[Embed-RL 论文](https://arxiv.org/html/2602.13823)。本文是本项目的实验方案，不声称完整复现该论文。

已对该数据版本的全部 39 个对比学习 JSON 文件进行文件头抽查：每个读取前 65,536 字节，提取完整记录，共 1,120 条，两端 CoT 均非空。它是前缀样本，**不是全量扫描、随机质量评估或媒体完整性验证**。执行时必须对实际使用的完整文件重新审计。

`pos_cot` 指这一训练配对的**正候选自身的 CoT**，不是所有检索候选都已标注。抽查未见 `neg_cot`。第一版使用其他配对的正候选作为批内负例，并复用这些候选的 CoT；暂不启用额外 hard negatives。

### 3.2 统一样本结构

实现规范化记录，至少包括以下字段。字段名可适配项目，语义必须保留。

```json
{
  "sample_id": "source-file:original-row-id",
  "dataset": "dataset-name",
  "task": "task-name-and-direction",
  "source_group_id": "source-media-or-query-group",
  "query": {
    "id": "query-id",
    "text": "original query text including task instruction",
    "images": [],
    "video_frames": [],
    "cot": "query_cot",
    "cot_provenance": "unknown"
  },
  "candidate": {
    "id": "canonical-candidate-id-within-task",
    "text": "original candidate text",
    "images": [],
    "video_frames": [],
    "cot": "pos_cot",
    "cot_provenance": "unknown"
  },
  "positive_candidate_ids": ["canonical-candidate-id-within-task"],
  "quality_flags": []
}
```

保留原始记录引用、清洗规则版本、媒体映射和拆分信息。`cot_provenance` 是执行器附加的审计信息，不能假设原始数据有该字段。

### 3.3 不同格式的映射

图像及文档数据一般使用 `qry / qry_image_path / pos_text / pos_image_path / query_cot / pos_cot`。视频数据使用嵌套 `qry / pos`，其中包含 `image / video / conversations`，两端 CoT 仍位于 `query_cot / pos_cot`。

视频记录必须根据任务选取实际输入，参照并核验[视频加载器](https://github.com/ZoengHN/Embed-RL/blob/1cb5b433395a709c1292df98d898083e75fb1057/train/contrastive_learning/dataset/mmeb/llavahound_dataset.py)：

- caption retrieval / QA：query 文本来自 qry 的 human 项，candidate 文本来自 pos 的 gpt 项；媒体按各端实际字段处理。
- video retrieval：query 文本来自 qry 的 gpt caption，candidate 文本来自 pos 的 human 任务提示，并读取候选视频帧。
- 不要把全部 human/gpt 会话串接起来；其中一端的答案或 caption 可能不属于该端的原始输入。

把 `images/blank.jpg` 及仅包含它的帧列表识别为无媒体占位符，不计为实际图像/视频。规范化 `<|image_1|>` 等旧式文本占位符，使用实际 backbone 的原生 processor 插入视觉 token，检查数量一致，避免重复插图或空路径残留占位符。

这些 JSON 主要提供路径和标注，不能假设含有全部原始媒体。按实际任务查明 MMEB、LLaVA-Hound、ViDoRe、VisRAG 等媒体来源；先复用本地文件或获取 pilot 所需部分。路径通过配置映射，禁止写死作者机器上的目录。媒体缺失须报告、剔除或阻塞对应任务，不能静默替换为空白图像继续宣称完成多模态训练。

### 3.4 清洗、候选规范化与泄漏检查

1. 对选定的完整 JSON 文件检查可解析性、双端原始输入、非空 CoT、媒体可读性、错误标记和重复情况；统计每个数据集的各类删除原因。
2. 主对照实验使用**同一份双端 CoT 可用的过滤后配对清单**。A 组不读取 CoT，B 组读取它。缺失 CoT 的额外数据可另存，首轮不混入任一组。
3. 不强制所有 CoT 满足固定标签格式或三个语义阶段。轻微标签缺失但正文可用的样本可保留并标记；空内容、错误响应、明显指令矛盾等需剔除或单独统计。不要自动改写正文造成未经记录的新训练集。
4. 对每个选定任务随机人工可读抽查至少 20 条，记录代表性错误。特别检查组合图像检索把“需要修改的目标”误认为“当前图像已满足”的情况。
5. 公共生成代码存在交替使用训练答案辅助 query CoT 的路径。过滤后行号奇偶不是可靠来源标记；不出现答案关键词也不能证明无答案辅助。保留 `unknown`，不伪造 provenance。参见[生成代码](https://github.com/ZoengHN/Embed-RL/blob/1cb5b433395a709c1292df98d898083e75fb1057/data_pipe/train_cot_generate/text_image_cot_generate_single.py)。
6. candidate ID 根据同一任务下的自身文本、媒体与必要任务指令建立，**不包含当前 query 或 CoT 文本**。同一候选多条 CoT 时，先审计是否依赖某个 query，再按可复现规则选取一份可独立复用的 CoT，记录合并数量。不要让同一候选在索引中因为不同 query 获得不同向量。
7. 训练/验证按原始媒体、文档、视频或 query 关联组拆分，并检查跨文件重复，例如 MSCOCO 不同方向。分类/VQA 中共享答案词不是划分连通组的理由；允许共享候选词表，禁止同一问题/媒体实例跨拆分。
8. 训练集允许答案辅助 CoT；正常验证集生成 CoT 时禁止把验证答案、正例标记、候选排名或相关性标签传给生成器。两者规则不同，必须记录。

## 4. 模型和输入实现

### 4.1 初始化策略

优先沿用本项目计划的 **VLM2Vec-V2 检索 checkpoint**。检查当前项目实际 checkpoint、模型配置和匹配的加载代码，固定权重 revision、代码 commit 和 processor 配置。

公开入口是 [VLM2Vec/VLM2Vec-V2.0](https://huggingface.co/VLM2Vec/VLM2Vec-V2.0)，模型页标记为 `qwen2_vl`；页面示例包含另一 checkpoint 名称，不能仅复制示例便认定加载了 V2。应根据实际文件确定是完整权重还是 adapter、所需 base checkpoint，以及原生检索 pooling。参考 [VLM2Vec 官方代码](https://github.com/TIGER-AI-Lab/VLM2Vec)，选择与权重匹配的版本，而非默认当前 main 必然兼容。

公开 Embed-RL 训练脚本采用的 Qwen3-VL 路径不能直接套到 Qwen2-VL/VLM2Vec。若现有设备无法运行 VLM2Vec-V2，可提供并运行明确标记的 `qwen3vl_2b_smoke` 工程配置，候选基座为 [Qwen/Qwen3-VL-2B-Instruct](https://huggingface.co/Qwen/Qwen3-VL-2B-Instruct)。这是工程冒烟替代方案，其结果不能冒充 VLM2Vec-V2 主实验；在最终报告说明主实验是否仍待运行。

所有相比较的训练组必须从**同一实际模型和同一初始参数状态**出发。不能比较 Qwen3-VL 教师与 VLM2Vec 无 CoT 基线并将差异归因于 CoT。

### 4.2 输入模板与 pooling

在模型原生多模态输入模板上实现两种模式：

```text
no_cot:
  原始任务指令 + 该端文本/媒体 + 原生检索 pooling 位置

both_cot:
  原始任务指令 + 该端文本/媒体
  Retrieval reasoning:
  该端 CoT 正文
  原生检索 pooling 位置
```

模板中的文字分隔符不需要变成新增特殊 token。CoT 应处于最终 pooling 位置之前，并沿因果注意力可见。原生 processor 处理图像、视频、位置编码和相关元数据，不能只拼 `input_ids` 后漏掉多模态参数。

优先复用 checkpoint 的原生 retrieval pooling/投影并做 L2 归一化。若实际模型必须新增 `<emb>`：统一给所有组添加，先扩展词表并固定初始化，再建立各组；确认该 token 的 embedding 行可训练且随 checkpoint 保存。padding 不得成为 pooling 位置，位置必须由 mask/标记明确确定，不能对变长批次盲取 `[:, -1]`。

第一版不要求 CoT 自回归生成，整段已有 CoT 作为输入，一次 teacher forward 即可计算最终 embedding。使用已有文本不等于已训练了 CoT 生成能力。

### 4.3 修复并证明 CoT 进入模型

已审计版本的加载器传递了 `cot` 字段，但 [MMEBUnifiedDataset._prepare_messages](https://github.com/ZoengHN/Embed-RL/blob/1cb5b433395a709c1292df98d898083e75fb1057/train/contrastive_learning/dataset/mmeb_unified_dataset.py) 的消息组装路径未消费该字段。不能据此推断论文作者实际实验没有使用 CoT；但执行本项目时必须检查并修复实际运行路径。

必须提供自动化测试和一份可读的实际输入抽样：

- 同一原始样本修改 query CoT，只改变 query 端 tokenization，candidate 端不变；反向同理。
- `no_cot` 输入不含 CoT 正文；`both_cot` 输入包含可解码识别的正文和有效 pooling 位置。
- 在 eval 模式相同权重下改变 CoT，非空有效样本的 hidden state/embedding 存在可测差异；不要求每次修改必然改善检索。
- 换 query 不改变同一个 candidate 的规范化输入与向量。
- 至少测试文本、实际图像和计划使用的视频/文档路径，不能只验证字符串拼接。
- tokenizer 截断后统计 CoT 保留 token 数；全被截断的样本视为输入失败，而不是有效 CoT 样本。

先计算视觉和原始文本预算，原始任务指令、媒体关联和末尾 pooling 必须保留；CoT 超预算时优先在句界保留前缀并记录截断，避免破坏视觉 token 展开。不能把整个序列盲目右截断。原始内容自身超过预算时，以同一规则处理两组或从共同清单排除。

## 5. 训练目标与可训练参数

### 5.1 双向、多正例对比学习

对每个真实对比批次，独立编码 query 和去重候选，得到归一化向量：

```text
S[i,j] = dot(e_query[i], e_candidate[j]) / temperature
L_q2d[i] = -logsumexp(S[i, known_positive_candidates])
             + logsumexp(S[i, valid_candidates])
L_d2q[j] = -logsumexp(S[known_positive_queries, j])
             + logsumexp(S[valid_queries, j])
L = (mean(L_q2d) + mean(L_d2q)) / 2
```

使用稳定的 logsumexp。为两种方向建立正确的 positive mask 和 valid mask；没有正例的行/列不参与对应均值并记录原因。相同答案标签、同一图像的多个描述和已有相关性标注不能被当成负例。仅在确认一对一且无重复时使用普通对角线交叉熵。

第一版优先按数据集/任务组成批次，避免无定义的跨任务负例及候选语义冲突。每个优化步按预设任务采样策略选任务。为重复标签较多的任务检查批内有效候选数量，不能得到几乎没有负例的“成功训练”。

分布式跨卡负例必须正确保留梯度、传递 candidate ID/positive mask，并测试与单进程等价实现的一致性。**梯度累积只扩大优化器有效 batch，不自动扩大对比学习负例池。** 分别记录每次损失实际使用的 query/candidate 数和每次 optimizer step 的样本数。

### 5.2 LoRA 起始配置

以下为本项目 pilot 起始值，不是论文结论或最优超参数：

- 视觉编码器和视觉连接模块先冻结；语言 backbone 的 attention/MLP 线性层使用 LoRA。
- LoRA rank 32，alpha 64，dropout 0.05；学习率 `2e-5`，AdamW，weight decay 0.01，warmup 比例 0.03，cosine schedule，梯度裁剪 1.0。
- 温度固定 `0.02`，即 logit scale 50；第一版不额外学习温度。
- 硬件支持时使用 bfloat16 和 gradient checkpointing。
- 实际对比池目标 64 对；可用多卡保梯度 gather 或正确实现的 GradCache。显存不足可降低，但 A/B 必须同改并报告。不要以 accumulation=64 谎报 64 个负例。
- 如 checkpoint 本身含 LoRA，明确现有 adapter 是否合并、继续训练或冻结后新增，验证重载结果。不要无记录地叠加适配器。

打印实际选中的 LoRA 模块名、可训练参数名/数量和视觉模块冻结状态。避免依据模糊字符串 `model` 或不匹配的 `vision_model` 属性声称只训练语言侧。模型不同，访问隐藏状态的代码应做适配，不能写死多层 `.model.model`。

## 6. 对照组与评测协议

### 6.1 必须完成的组

| 组 | 初始化 | 训练输入 | 正常评测输入 | 用途 |
|---|---|---|---|---|
| Base | 原始检索 checkpoint | 不训练 | 两端原始输入 | 检查基座水平 |
| A：NoCoT-FT | 同一初始状态 | 两端原始输入 | 两端原始输入 | 排除继续训练本身的收益 |
| B：CoT-Teacher | 同一初始状态 | 两端原始输入 + 各自训练 CoT | 两端原始输入 + 各自独立生成的 CoT | 检查 CoT 增强收益 |
| B-off | B 的同一 checkpoint | 不另训练 | 两端原始输入 | 检查移除 CoT 的影响 |

Base 使用预训练 checkpoint 原生、已验证的 pooling。若工程替代基座没有原生 embedding 能力，需标注 Base 只是未训练检索初始化；必要时另报告加入新 pooling 后的初始值，不能混同原生 VLM2Vec 基线。

A/B 保持过滤清单、数据顺序、任务比例、视觉处理、负例池、优化器更新数、LoRA 范围和随机种子一致；先保存一份公共初始化再分别训练。CoT 会增加 token 和计算成本，报告实际时间与 token 数，称为“相同数据/更新预算”，不要声称严格同 FLOPs。

### 6.2 正常评测 CoT 的生成

教师编码器与 CoT 生成器是两个角色。正常验证所需 CoT 可以由已有独立生成模型离线产生；不能假设本次仅对比训练的教师已经会生成 CoT。

生成器只访问该端原始内容和任务指令：

```text
query_cot_eval = Generator(query 本身)
candidate_cot_eval = Generator(candidate 本身)
```

candidate CoT 按独立候选 ID 缓存，**整个评测候选库**均需处理，不能仅为当前 query 的正例生成。不要将候选集、参考答案、配对信息或评分反馈放入生成 prompt。对候选本身是答案词的任务，读取该词属于读取 candidate 自身内容，不能把它再提供给 query 生成器。

建议基础 prompt（按实际输入角色适配，不加入 ground truth）：

```text
Given only this retrieval endpoint and its task instruction, describe the
observable content and reason about the semantic information useful for
retrieval. Preserve important attributes, relations, and constraints. Do not
assume access to another endpoint, a candidate list, or a reference answer.
Do not invent visual evidence. Return concise reasoning grounded in this input.
```

生成模型 ID/revision、prompt、采样参数、媒体预算和失败重试规则写入 manifest。第一版默认 temperature 0、最大生成 512 tokens；选择环境中已有且支持所需模态的模型，不擅自调用收费服务。所有比较使用同一份生成缓存。

从公开训练数据划出的验证记录，其自带 CoT 来源可能含答案辅助。可另做 `privileged_cot_diagnostic`，但必须单独命名，不能当作正常 held-out 指标、主模型选择依据或正式 benchmark 结果。没有可用生成器时可完成其余步骤，并明确“正常 CoT 评测待完成”，不能用这个诊断替代。

### 6.3 数据、指标与结论

- 优先使用官方验证协议；没有可用验证划分时，从训练来源按组固定留出约 5%，pilot 至少准备每个任务约 200 个 query（不足时如实报告）。这些数据不进入训练。
- 同一任务所有组使用相同 query、完整规定候选库和相关性标签。不得用训练小 batch 内命中率冒充检索评测。
- 优先沿用各 benchmark 的官方主指标，如其规定的 Recall/Hit@1 或 nDCG。额外报告 Recall@1/5/10 只在该任务定义适用时使用。
- 报告逐任务结果、等权任务宏平均、模态分组、B-A 差值、速度/显存与 CoT 开销。pilot 子集结果不得声称代表完整 MMEB-V2。
- 用同一批 query/媒体组做配对 bootstrap，报告差值区间；单随机种子和相关 query 下的区间不能替代多种子复现。
- 验证 B 优于 A 时，也要检查 B-off、逐任务退化及错误样例。可增加同任务错配 CoT 的小规模诊断，但它不是第一版必需训练组。
- 仅最终 embedding 变好不能证明“分块过程有意义”，也不能证明学生能够保留收益；这些属于后续蒸馏实验。

## 7. 执行顺序与边界

### 阶段 A：环境、适配和数据

1. 阅读项目指引，检查现有实现、CUDA/GPU、可用显存、磁盘、模型和媒体。输出 `environment_report.md`。
2. 确定实际 backbone、processor、原生 pooling 和依赖版本，完成原始 checkpoint 的可重复 encoding smoke。记录模型解析结果，不只记录用户输入的别名。
3. 实现统一数据适配、候选 ID、多正例标注、固定拆分和 CoT 可见性测试。
4. pilot 默认选择有真实媒体的 2–3 个任务，优先图文检索与视觉 QA，资源允许再加入文档或视频。总训练配对上限 20,000，按任务均衡抽样，种子 42；不能只取每个文件开头。较少可用样本时按真实规模执行。
5. 列出实际选定任务、完整文件扫描结果、媒体缺失率、质量过滤、重复与拆分审计。未覆盖模态在报告注明，不虚构支持程度。

### 阶段 B：工程冒烟

1. 用真实数据小批次各运行 A/B 至少 10 个优化步，检查有限 loss、非零梯度和可训练参数更新。
2. 保存并重载 checkpoint，在 eval 模式比较同样输入的向量，给出实际 dtype 下的容差和误差。
3. 测试小候选库离线编码、去重和准确的多正例指标。
4. 若采用分布式/GradCache，完成对应的损失和梯度一致性测试后再使用。
5. 冒烟是工程检查，不要求 10 步内稳定提升，也不能用它作为科研收益证据。

### 阶段 C：pilot 对照

1. 固定公共初始化和数据/批次计划，分别训练 A/B 一轮，最多 1,000 个 optimizer steps，以先达到者为准。实际步数要相同。
2. 默认只用最后 checkpoint 做首轮比较；如使用验证选模，A/B 必须采用同样的固定频率、选择规则和正常验证信息。不可用测试集挑 checkpoint。
3. 生成无答案辅助的验证 CoT，完成 Base/A/B/B-off。若生成器尚不可用，完成训练与独立输入审计，报告评测缺口。
4. 输出结果和样例，判断是输入错误、数据噪声、优化问题，还是当前方案未显示收益。不要为了得到正结果无限调参。

### 阶段 D：扩大实验

pilot 正常评测显示有价值、且现有资源支持时，再制定并记录完整训练规模；准备可恢复的全量配置。首份文档授权范围内优先完成上述有界 pilot，不自动启动无时间上限的全量训练。主要科研结论需要后续更多任务和至少 3 个种子验证。

## 8. 配置和命令接口

实现如下配置语义。`null` 表示执行器需要从当前环境解析并写入 `resolved.yaml`，不是训练时容忍缺失的占位值。

```yaml
experiment:
  name: teacher_pilot
  seed: 42
  mode: both_cot               # A 使用 no_cot，B 使用 both_cot
model:
  family: vlm2vec_v2
  name_or_path: VLM2Vec/VLM2Vec-V2.0
  revision: null              # 下载/加载前解析并固定
  backbone: null              # 从真实 config 验证
  pooling: native
  normalize: true
  freeze_vision: true
  freeze_visual_connector: true
  lora_rank: 32
  lora_alpha: 64
  lora_dropout: 0.05
data:
  repo_id: ZoengHouNaam/Embed-RL-Train
  revision: af9e2e062e51f79df926909e7b9481fa697bfbcf
  subdir: MMEB-train-contrastive-learning
  selected_tasks: null
  media_roots: null
  max_train_pairs: 20000
  validation_group_fraction: 0.05
  require_both_cot: true
  canonicalize_candidates: true
  sampler: task_balanced_same_task_batches
input:
  max_sequence_tokens: 4096
  max_cot_tokens: 1024
  image_budget: native_checkpoint_default
  max_video_frames: 8
  truncation: preserve_original_and_pool_then_cot_prefix
training:
  max_epochs: 1
  max_optimizer_steps: 1000
  learning_rate: 2.0e-5
  weight_decay: 0.01
  warmup_ratio: 0.03
  scheduler: cosine
  gradient_clip_norm: 1.0
  precision: bf16_if_supported
  temperature: 0.02
  contrastive_pool_target_pairs: 64
  microbatch_pairs: null
  gradient_accumulation_steps: null
  negative_pool_backend: null # local / differentiable_gather / gradcache
  multi_positive: true
evaluation:
  cot_source: independent_generator
  generator_model: null
  generator_revision: null
  generator_temperature: 0
  generator_max_new_tokens: 512
  allow_reference_answers: false
  candidate_encoding: independent_cached
  checkpoint_selection: last
process_export:
  enabled: false
  chunks: 3
  boundary: sentence_near_equal_token_count
  state: causal_chunk_end_last_layer
```

4096 是 pilot 起始序列预算，须结合实际视觉展开和显存检查；不能因写在配置里便假设所有原始样本都能容纳。所有自动降低的预算、实际 batch、任务选择和 checkpoint 解析结果都写入 `resolved.yaml`。A/B 共享这些值。

建议提供以下模块命令；若已有框架，交付功能等价且实际可执行的命令：

```bash
python -m teacher_pipeline.audit --config configs/teacher_pilot.yaml
python -m teacher_pipeline.prepare --config configs/teacher_pilot.yaml
python -m teacher_pipeline.smoke --config configs/teacher_pilot.yaml
python -m teacher_pipeline.train --config configs/teacher_pilot.yaml --mode no_cot
python -m teacher_pipeline.train --config configs/teacher_pilot.yaml --mode both_cot
python -m teacher_pipeline.generate_eval_cot --config configs/teacher_pilot.yaml
python -m teacher_pipeline.evaluate --config configs/teacher_pilot.yaml --suite base,a,b,b_off
python -m teacher_pipeline.report --config configs/teacher_pilot.yaml
```

每个命令支持 `--help`，失败时返回非零状态；训练支持 resume，缓存区分 split、model revision、processor、模板、CoT 模式与生成版本。A/B 各用独立输出目录，公共初始化和批次计划使用可追溯的共享文件。

## 9. 为后续过程蒸馏预留的接口

教师验证后，提供一个默认关闭的小样本状态导出接口：

```text
encode_with_process(endpoint, cot, K=3)
  -> final_embedding
  -> chunk_end_hidden_states[K, hidden_size]
  -> chunk_mask[K]
  -> token_spans / original_text_spans / layer_id / provenance
```

在同一条完整因果序列上，以句子/段落边界附近的近等 token 长度切块，读取**各块最后一个有效正文 token 的隐藏状态**。这些状态可看到原始输入和此前 CoT 前缀；不能将各块独立编码后声称它们是累积状态。

使用实际 processor 完成后的 token 位置，排除 padding、角色标记和末尾 pooling token；验证改动后续 CoT 不影响此前块末状态（eval 模式、允许数值容差）。若 backbone 实际采用双向文本注意力，先明确该限制，不能直接声称具有前缀因果性。

短 CoT 使用更少有效块并输出 mask，不复制最后状态凑满 K。记录截断后的真实边界。只导出少量诊断样本，不默认缓存全训练集的所有层状态。报告范数、块间相似度和样例，但不将“状态不同”等同于“具有可解释推理能力”。

## 10. 交付和验收

最终至少交付：

- 可运行代码、固定依赖、模型适配和真实执行命令。
- `configs/teacher_pilot.yaml`、公共初始化说明、各组 `resolved.yaml`。
- `environment_report.md`、`data_audit.json`、固定 split/候选清单、样本过滤记录。
- CoT 输入可见性、端点独立性、多正例 loss、保存重载等测试及真实结果。
- A/B checkpoint、tokenizer/processor、LoRA 和 pooling 所需全部参数；若未训练则明确原因。
- 正常验证的逐任务指标与 `teacher_report.md`，包括成本、失败案例和限制。
- 可选状态导出接口及少量示例；不要求学生训练代码。

把验收分成两类，不能混淆：

**工程完成：** 数据和媒体路径正确，CoT 确实输入，损失/梯度与正例 mask 正确，候选独立编码，训练可恢复，模型可重载，评测无参考答案输入。任何未满足项目需明确标记。

**科研判断：** 正常 held-out 协议下 B 相对 A 是否获得可信收益，哪些任务改善或退化，是否值得进入过程蒸馏。没有收益仍然是有效实验结果；此时交付结果与下一项最有信息量的排查建议，不宣布“教师已成功增强检索”。

执行完用简洁中文报告实际做了什么、跑到哪一步、结果数值、产物位置和剩余阻塞项。严禁把计划中的命令写成已执行，把未下载全量数据写成完成全量审计，或把答案辅助 CoT 的诊断值写成正常检索成绩。
